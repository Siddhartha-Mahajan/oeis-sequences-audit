# Audit Notes

## A000410

- New rigorous frontier interval
  `20632852027790990462837 <= a(10) <= 115875324413944408596657`.
- Lower side: disjoint primitive-normal corank-one sections through coefficient
  cutoff 4 and support 8. Upper side: exact union bound over projective
  hyperplanes over `F_7`, best among tested primes `2,3,5,7,11,13`.

## A000576

- Corrected the name from normalized to reduced Latin rectangles.
- Proved the first explicit frontier interval
  `167650325863480269832990259815752180695040 <= a(12) <= 42335447246821837670204841011691110328906547200`
  by starting with the exact published `R_{6,12}` and applying permanent
  bounds to the remaining four rows.

## A000157

- Completed definition correction and exact values `a(12)` through `a(16)`.

## A001072

- Completed exact term `a(15)=41142` with an independently checked graph6
  certificate.

## A002887

- Completed correction `a(5)=48` and exact extension through `a(10)`.

## A003167

- Focus on this sequence.

## A005281

- Put this and all bounds-increase sequences in a separate folder: A005281,
  A005312, A202140, A306795, A358784, A365910, and A380991.

## A005787

- Reachable-circle configuration count. Push this sequence further; it appears
  important.

## A006545

- Completed: exact all-`n` OGF, finite coefficient sample, and independent graph
  verification through `n=12`.

## A007234

- Completed: exact all-`n` conjugacy-type recurrence, corrected published
  `a(7),a(8)` to `3390,29409`, and a finite recurrence sample. Three direct
  functional-graph algorithms and explicit witnesses independently agree.

## A007847

- Completed a new certified lower bound
  `a(10)>=5013843621741086` using exact bounded-normal enumeration through
  coefficient 12, affine-rank checks, symmetry-orbit accounting, and a
  support decomposition based on the exact terms through dimension 9.

## A009997

- Proved the all-`n` lower recurrence `a(n+1)>=2^(n-1)*a(n)`, giving the new
  explicit frontier bound `a(8)>=13733158592` from the published `a(7)`.

## A273354

- This seems fun; try to determine or disprove `a(3)`.

## A323134

- This sequence seems underdefined.

## A323560

- This sequence seems unimportant, but that does not mean it should be removed.

## A363253

- Completed the two missing internal terms: `a(6)=a(7)=-1`.
- The proof combines exact finite-prefix subset-sum counts with conductor
  intervals showing that all later polygonal targets have too many
  representations.

## A363253

- Why is this entry included?

## A368355

- This should be split into three separate folders because it combines the
  companion sequences A368353, A368354, and A368355.
