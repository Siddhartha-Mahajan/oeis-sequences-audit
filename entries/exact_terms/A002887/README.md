# A002887

This folder corrects and extends the sequence:

```text
a(5)=48, a(6)=48, a(7)=122, a(8)=122, a(9)=264, a(10)=264.
```

The published `a(5)=50` is not minimal. Exact center-path profile enumeration
supplies the lower bounds; explicit edge-list trees and a direct verifier
supply the matching constructions.

Run `python3 scripts/replay_all.py` for the complete replay.
