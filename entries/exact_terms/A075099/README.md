# A075099 reproducibility package

This folder proves five exact values:

```text
a(7)=151, a(8)=276, a(9)=556, a(10)=1066, a(12)=4171.
```

It also stores a time-limited `n=11` construction with total cost 2138, which
is not claimed exact. Since `a(11)` remains unknown, `a(12)` belongs in an
OEIS comment rather than after a gap in the DATA field. See the
[short manuscript](MANUSCRIPT.md) for the model, replay commands, and
certificate boundary.
