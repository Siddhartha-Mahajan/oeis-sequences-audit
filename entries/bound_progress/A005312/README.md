# A005312 reproducibility package

This folder certifies the constructive lower bound `a(8)>=2670`. It does not
claim that 2670 is optimal.

```bash
python3 scripts/verify_matrix.py certificates/n8_witness_2670.txt
```
