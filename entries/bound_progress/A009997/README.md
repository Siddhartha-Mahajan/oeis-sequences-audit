# A009997

This folder proves the all-dimensions lower recurrence

```text
a(n+1) >= 2^(n-1)*a(n)  for n>=1,
```

and hence the new frontier bound

```text
a(8) >= 13733158592.
```

The argument is purely mathematical; no computation or certificate is needed.
