# All-$n$ bounds

This category contains theorem-level inequalities valid in every dimension
that do not determine the corresponding sequence exactly.

1. **A009997.** Ternary chamber lifting proves
   `a(n+1)>=((3^(n-1)+1)/2)*a(n)` for every `n>=1`, and hence
   `a(8)>=78321920095`.

